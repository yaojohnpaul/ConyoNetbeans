/* Comment */
OMG floaty TEST makeKuha 300.2 db

hireYaya test1 bayad (inty i, floaty j, stringy k) makeBalik poor
$
        makeSulat(i) db
$

hireYaya test2 bayad (inty a) makeBalik poor
$
    likeKapag(a < 0)
    $
        makeSulat("Negative number!") db
    $
    thisNalangKapag(a == 0)
    $
        makeSulat("Zero!") db
    $
    thisNalang
    $
        makeSulat("Positive number!") db
    $
$

hireYaya recursion bayad (inty a) makeBalik inty i
$
    likeKapag(a == 0)
    $
        i makeKuha 0 db
    $
    thisNalang
    $
        i makeKuha a + makeTawag recursion(a-1) db
    $
$

superYaya
$
        makeSulat(makeTawag recursion(10)) db
$